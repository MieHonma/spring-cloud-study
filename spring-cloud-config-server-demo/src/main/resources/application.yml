server:
  port: 9090
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///D:/spring-cloud-config
management:
  security:
    enabled: false